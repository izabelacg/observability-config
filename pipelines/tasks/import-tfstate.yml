---
platform: linux
image_resource:
  type: registry-image
  source: {repository: concourse/unit}

inputs:
  - name: greenpeace
outputs:
  - name: tfstate

params:
  RESOURCE_ADDRESS:
  RESOURCE_ADDRESS_SYSSTAS:
  DATADOG_API_KEY:
  DATADOG_APP_KEY:
  DASHBOARD_TITLE:

run:
  path: greenpeace/pipelines/tasks/scripts/import-tfstate
